<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/fragment"
    tools:context=".ui.playlist.PlaylistFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/playlist_rc"
        style="@style/fragment_recycler_view"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/playlist_action"
        tools:listitem="@layout/fragment_playlist_item" />

    <LinearLayout
        android:id="@+id/playlist_action"
        style="@style/fragment_actions"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <Button
            android:id="@+id/playlist_create"
            style="@style/button"
            android:text="@string/create_playlist" />

        <!--        TODO - Remove add today in favor of plus/add button on indivadual items (in place of radio button)? -->
        <Button
            android:id="@+id/playlist_add"
            style="@style/button"
            android:text="@string/add_playlist" />

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>